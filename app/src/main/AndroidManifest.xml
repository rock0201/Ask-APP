<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ask">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:name=".utils.MyApplication"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity">
        </activity>
        <activity android:name=".MainviewActivity"
           >
        </activity>
        <activity android:name=".QwithresultActivity"></activity>
        <activity android:name=".ResultDetail"></activity>
        <activity android:name=".CreatQuestion"></activity>
        <activity android:name=".CreatResult"></activity>
        <activity android:name=".UserQuetion"></activity>
        <activity android:name=".UserAnswer"></activity>
    </application>

</manifest>